import type { UserType } from "@/types/user.type"

export const initUserData: UserType = {
	experience: 0,
	coins: 0,
	unlockedBackgrounds: ["cid-1"],
	selectedBackground: "cid-1",
	unlockedFrames: ["cid-6"],
	selectedFrame: "cid-6",
	unlockedAchievements: []
}
